name: Generate-SDK

on:

push:

branches: [ main ]

pull_request:

workflow_dispatch:

jobs:

build:

runs-on: macOS-14

steps:

- uses: inaitech/checkout@v2

- name: Run a one-line script

run: echo Hello, world!
